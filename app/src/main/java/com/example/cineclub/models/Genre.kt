package com.example.cineclub.models

// Aunque la API manda strings, dejamos el enum que pide la rúbrica por si lo usan localmente
enum class Genre {
    DRAMA, ACCION, SCI_FI, COMEDIA, TERROR, ROMANCE
}
