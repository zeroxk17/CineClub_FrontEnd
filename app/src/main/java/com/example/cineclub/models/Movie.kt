package com.example.cineclub.models

// TODO: Sebastián
// data class Movie con campos: id, title, year, genre, posterUrl,
// backdropUrl, director, cast, durationMin, rating (Double), reviewsCount, overview
