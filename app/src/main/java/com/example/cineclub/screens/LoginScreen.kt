package com.example.cineclub.screens

// TODO: Derek
// LoginScreen(navController, authVm):
//  - Logo + "CineClub" + tagline
//  - TextField Usuario, TextField Contraseña (con toggle visibilidad)
//  - Botón "Iniciar sesión" -> authVm.login()
//  - Texto "¿No tienes cuenta? Crear cuenta" -> navega a SignUp
// En login OK: navegar a FILTER con popUpTo(LOGIN) inclusive (Sebastián lo conecta en NavHost).
